<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proyección Óptica</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #0a0a0a;
            color: #ffffff;
            overflow-x: hidden;
        }

        header {
            background: rgba(0, 0, 0, 0.95);
            padding: 20px 0;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            box-shadow: 0 2px 30px rgba(139, 92, 246, 0.3);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(139, 92, 246, 0.2);
        }

        nav {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 40px;
        }

        .logo {
            font-size: 32px;
            font-weight: 700;
            background: linear-gradient(135deg, #8b5cf6, #ec4899);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: 1px;
        }

        .nav-links {
            display: flex;
            gap: 40px;
            list-style: none;
        }

        .nav-links a {
            text-decoration: none;
            color: #ffffff;
            font-weight: 500;
            transition: all 0.3s ease;
            position: relative;
            font-size: 16px;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -5px;
            left: 0;
            background: linear-gradient(135deg, #8b5cf6, #ec4899);
            transition: width 0.3s ease;
        }

        .nav-links a:hover {
            color: #8b5cf6;
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a0a2e 100%);
        }

        .hero-bg {
            position: absolute;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            z-index: 0;
        }

        .hero-bg::before {
            content: '';
            position: absolute;
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, rgba(139, 92, 246, 0.3) 0%, transparent 70%);
            top: -200px;
            right: -200px;
            border-radius: 50%;
            animation: pulse 4s ease-in-out infinite;
        }

        .hero-bg::after {
            content: '';
            position: absolute;
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(236, 72, 153, 0.2) 0%, transparent 70%);
            bottom: -250px;
            left: -250px;
            border-radius: 50%;
            animation: pulse 5s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
                opacity: 0.5;
            }
            50% {
                transform: scale(1.1);
                opacity: 0.8;
            }
        }

        .hero-content {
            position: relative;
            z-index: 1;
            text-align: center;
            padding: 0 20px;
            opacity: 0;
            animation: fadeInUp 1.2s ease forwards;
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .hero h1 {
            font-size: 72px;
            margin-bottom: 20px;
            font-weight: 700;
            background: linear-gradient(135deg, #ffffff, #8b5cf6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            letter-spacing: 2px;
        }

        .hero p {
            font-size: 28px;
            margin-bottom: 50px;
            color: #a0a0a0;
            font-weight: 300;
        }

        .btn {
            display: inline-block;
            padding: 18px 50px;
            background: linear-gradient(135deg, #8b5cf6, #ec4899);
            color: white;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.4s ease;
            box-shadow: 0 10px 30px rgba(139, 92, 246, 0.4);
            font-size: 18px;
        }

        .btn:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 15px 40px rgba(139, 92, 246, 0.6);
        }

        .floating-particles {
            position: absolute;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            z-index: 0;
        }

        .particle {
            position: absolute;
            width: 3px;
            height: 3px;
            background: rgba(139, 92, 246, 0.6);
            border-radius: 50%;
            animation: float 10s ease-in-out infinite;
        }

        .particle:nth-child(1) { top: 10%; left: 15%; animation-delay: 0s; }
        .particle:nth-child(2) { top: 30%; left: 80%; animation-delay: 2s; }
        .particle:nth-child(3) { top: 70%; left: 25%; animation-delay: 4s; }
        .particle:nth-child(4) { top: 50%; left: 70%; animation-delay: 1s; }
        .particle:nth-child(5) { top: 80%; left: 50%; animation-delay: 3s; }

        @keyframes float {
            0%, 100% {
                transform: translateY(0) translateX(0);
            }
            25% {
                transform: translateY(-30px) translateX(20px);
            }
            50% {
                transform: translateY(-60px) translateX(-20px);
            }
            75% {
                transform: translateY(-30px) translateX(30px);
            }
        }

        section {
            padding: 120px 40px;
            max-width: 1400px;
            margin: 0 auto;
        }

        .services {
            background: #0a0a0a;
            position: relative;
        }

        .section-title {
            text-align: center;
            font-size: 52px;
            margin-bottom: 80px;
            font-weight: 700;
            background: linear-gradient(135deg, #ffffff, #8b5cf6);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            position: relative;
            padding-bottom: 30px;
        }

        .section-title::after {
            content: '';
            position: absolute;
            width: 100px;
            height: 4px;
            background: linear-gradient(135deg, #8b5cf6, #ec4899);
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 2px;
        }

        .service-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 40px;
            margin-top: 60px;
        }

        .service-card {
            background: linear-gradient(135deg, #1a1a1a 0%, #2a1a3e 100%);
            border-radius: 24px;
            overflow: hidden;
            transition: all 0.4s ease;
            cursor: pointer;
            border: 1px solid rgba(139, 92, 246, 0.2);
            opacity: 0;
            transform: translateY(30px);
        }

        .service-card.visible {
            animation: fadeInUp 0.8s ease forwards;
        }

        .service-card:hover {
            transform: translateY(-15px) scale(1.02);
            box-shadow: 0 20px 60px rgba(139, 92, 246, 0.4);
            border-color: rgba(139, 92, 246, 0.6);
        }

        .service-image {
            width: 100%;
            height: 220px;
            background-size: cover;
            background-position: center;
            position: relative;
        }

        .service-image::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 50%;
            background: linear-gradient(to top, rgba(26, 26, 26, 1), transparent);
        }

        .service-content {
            padding: 35px;
        }

        .service-icon {
            width: 70px;
            height: 70px;
            background: linear-gradient(135deg, #8b5cf6, #ec4899);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 25px;
            font-size: 32px;
            box-shadow: 0 8px 20px rgba(139, 92, 246, 0.4);
        }

        .service-card h3 {
            font-size: 26px;
            margin-bottom: 15px;
            color: #ffffff;
            font-weight: 600;
        }

        .service-card p {
            color: #b0b0b0;
            line-height: 1.7;
            font-size: 16px;
        }

        .gallery {
            background: #0a0a0a;
            padding: 120px 40px;
        }

        .gallery-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 60px;
        }

        .gallery-item {
            height: 350px;
            border-radius: 20px;
            overflow: hidden;
            position: relative;
            cursor: pointer;
            transition: all 0.4s ease;
            border: 1px solid rgba(139, 92, 246, 0.2);
        }

        .gallery-item:hover {
            transform: scale(1.05);
            box-shadow: 0 20px 50px rgba(139, 92, 246, 0.5);
            border-color: rgba(139, 92, 246, 0.6);
        }

        .gallery-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: all 0.4s ease;
        }

        .gallery-item:hover img {
            transform: scale(1.1);
        }

        .gallery-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            padding: 30px;
            background: linear-gradient(to top, rgba(0, 0, 0, 0.9), transparent);
            transform: translateY(100%);
            transition: all 0.4s ease;
        }

        .gallery-item:hover .gallery-overlay {
            transform: translateY(0);
        }

        .gallery-overlay h4 {
            font-size: 22px;
            color: #ffffff;
            margin-bottom: 8px;
        }

        .gallery-overlay p {
            color: #b0b0b0;
            font-size: 14px;
        }

        .contact {
            background: linear-gradient(135deg, #1a0a2e 0%, #0a0a0a 100%);
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .contact::before {
            content: '';
            position: absolute;
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(139, 92, 246, 0.15) 0%, transparent 70%);
            top: -300px;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 50%;
        }

        .contact-info {
            display: flex;
            justify-content: center;
            gap: 80px;
            margin-top: 60px;
            flex-wrap: wrap;
            position: relative;
            z-index: 1;
        }

        .contact-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
            transition: all 0.3s ease;
        }

        .contact-item:hover {
            transform: translateY(-10px);
        }

        .contact-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, #8b5cf6, #ec4899);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
            box-shadow: 0 10px 30px rgba(139, 92, 246, 0.5);
        }

        .contact-item a {
            color: #ffffff;
            text-decoration: none;
            font-size: 20px;
            transition: all 0.3s ease;
            font-weight: 500;
        }

        .contact-item a:hover {
            color: #8b5cf6;
            transform: scale(1.1);
        }

        @media (max-width: 768px) {
            .hero h1 {
                font-size: 42px;
            }

            .hero p {
                font-size: 20px;
            }

            .nav-links {
                display: none;
            }

            .service-grid,
            .gallery-grid {
                grid-template-columns: 1fr;
            }

            .section-title {
                font-size: 36px;
            }

            nav {
                padding: 0 20px;
            }

            section {
                padding: 80px 20px;
            }
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <div class="logo">Proyección Óptica</div>
            <ul class="nav-links">
                <li><a href="#inicio">Inicio</a></li>
                <li><a href="#servicios">Servicios</a></li>
                <li><a href="#galeria">Galería</a></li>
                <li><a href="#contacto">Contacto</a></li>
            </ul>
        </nav>
    </header>

    <section id="inicio" class="hero">
        <div class="hero-bg"></div>
        <div class="floating-particles">
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
        </div>
        <div class="hero-content">
            <h1>Proyección Óptica</h1>
            <p>Tu visión es nuestra misión</p>
            <a href="#contacto" class="btn">Contáctanos</a>
        </div>
    </section>

    <section id="servicios" class="services">
        <h2 class="section-title">Nuestros Servicios</h2>
        <div class="service-grid">
            <div class="service-card">
                <div class="service-image" style="background-image: url('https://images.unsplash.com/photo-1574258495973-f010dfbb5371?w=600&h=400&fit=crop');"></div>
                <div class="service-content">
                    <div class="service-icon">👓</div>
                    <h3>Gafas Oftálmicas</h3>
                    <p>Contamos con amplias monturas para correccion.</p>
                </div>
            </div>
            <div class="service-card">
                <div class="service-image" style="background-image: url('https://images.unsplash.com/photo-1511499767150-a48a237f0083?w=600&h=400&fit=crop');"></div>
                <div class="service-content">
                    <div class="service-icon">🕶️</div>
                    <h3>Gafas de Sol</h3>
                    <p>Protección UV total con diseños elegantes y modernos. Colecciones exclusivas para cada temporada.</p>
                </div>
            </div>
            <div class="service-card">
                <div class="service-image" style="background-image: url('https://images.unsplash.com/photo-1583912267550-28e88c7b6089?w=600&h=400&fit=crop');"></div>
                <div class="service-content">
                    <div class="service-icon">👁️</div>
                    <h3>Examen Visual</h3>
                    <p>Evaluaciones completas con consultas completas y de calidad.</p>
                </div>
            </div>
            <div class="service-card">
                <div class="service-image" style="background-image: url('https://images.unsplash.com/photo-1614013363098-b671d992413f?w=600&h=400&fit=crop');"></div>
                <div class="service-content">
                    <div class="service-icon">⚡</div>
                    <h3>Lentes de Contacto</h3>
                    <p>Lentes de contacto de buena calidad.</p>
                </div>
            </div>
            <div class="service-card">
                <div class="service-image" style="background-image: url('https://images.unsplash.com/photo-1622396636295-c32e7e1e9b8d?w=600&h=400&fit=crop');"></div>
                <div class="service-content">
                    <div class="service-icon">🔧</div>
                    <h3>Reparaciones</h3>
                    <p>Servicio de reparacion de monturas.</p>
                </div>
            </div>
            <div class="service-card">
                <div class="service-image" style="background-image: url('https://images.unsplash.com/photo-1556306535-38febf6782e7?w=600&h=400&fit=crop');"></div>
                <div class="service-content">
                    <div class="service-icon">✨</div>
                    <h3>Asesoría Personalizada</h3>
                    <p>Te ayudamos a encontrar una buena montura con la variedad que manejamos.</p>
                </div>
            </div>
        </div>
    </section>

    <section id="galeria" class="gallery">
        <h2 class="section-title">Nuestra Galería</h2>
        <div class="gallery-grid">
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1575698970551-88c59fb5e027?w=500&h=500&fit=crop" alt="Monturas elegantes">
                <div class="gallery-overlay">
                    <h4>Diseños Exclusivos</h4>
                    <p>Las mejores marcas del mercado</p>
                </div>
            </div>
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1591076482161-42ce6da69f67?w=500&h=500&fit=crop" alt="Lentes modernos">
                <div class="gallery-overlay">
                    <h4>Tendencias 2024</h4>
                    <p>Lo último en moda óptica</p>
                </div>
            </div>
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1608056589134-5a8c3cdb3f72?w=500&h=500&fit=crop" alt="Óptica moderna">
                <div class="gallery-overlay">
                    <h4>Tecnología Avanzada</h4>
                    <p>Equipos de última generación</p>
                </div>
            </div>
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1607452262926-c91a1f6ce4f3?w=500&h=500&fit=crop" alt="Gafas de sol premium">
                <div class="gallery-overlay">
                    <h4>Protección UV</h4>
                    <p>Gafas de sol premium</p>
                </div>
            </div>
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1629362185706-981eedae56b0?w=500&h=500&fit=crop" alt="Colección diversa">
                <div class="gallery-overlay">
                    <h4>Variedad Infinita</h4>
                    <p>Para todos los gustos</p>
                </div>
            </div>
            <div class="gallery-item">
                <img src="https://images.unsplash.com/photo-1516069677018-378515003435?w=500&h=500&fit=crop" alt="Detalle de calidad">
                <div class="gallery-overlay">
                    <h4>Calidad Premium</h4>
                    <p>Atención al detalle</p>
                </div>
            </div>
        </div>
    </section>

    <section id="contacto" class="contact">
        <h2 class="section-title">Contáctanos</h2>
        <div class="contact-info">
            <div class="contact-item">
                <div class="contact-icon">📱</div>
                <a href="tel:3165764245">316 576 4245</a>
            </div>
            <div class="contact-item">
                <div class="contact-icon">📧</div>
                <a href="mailto:Isacruz7715@gmail.com">Isacruz7715@gmail.com</a>
            </div>
        </div>
    </section>

    <script>
        const observerOptions = {
            threshold: 0.15,
            rootMargin: '0px 0px -80px 0px'
        };

        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.service-card').forEach(card => {
            observer.observe(card);
        });

        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        const particles = document.querySelectorAll('.particle');
        particles.forEach((particle, index) => {
            particle.style.animationDelay = index * 0.5 + 's';
        });
    </script>
</body>
</html>
